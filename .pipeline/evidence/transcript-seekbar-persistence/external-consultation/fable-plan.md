{"verdict":"SHOULD_FIX","summary":"The root-cause analysis (scrollIntoView propagating to ancestor scroll containers) and the fix direction (container-local scrollTo via a testable helper) are sound. However, the new helper `transcript-scroll.ts` and its test are untracked files whose contents are not in the supplied diff, so the center/clamp math, coordinate basis, and smooth-scroll behavior cannot be verified from this brief. A few concrete risks should be checked before merging: coordinate calculation must be relative to the actual scrolling element (offsetTop can be relative to a different offsetParent), the previous `behavior: \"smooth\"` should be preserved intentionally or its removal documented, and the test should cover the bottom-end clamp in addition to the top clamp.","confidence":"medium","findings":[{"id":"F1","severity":"SHOULD_FIX","title":"Helper and test contents are unverifiable from the provided context","evidence":"git status shows `?? services/dashboard/src/lib/transcript-scroll.ts` and `?? services/dashboard/tests/test_transcript_scroll.test.ts` as untracked; the diff excerpt only covers transcript-viewer.tsx, so the centering/clamp logic under review is not visible.","recommendation":"Confirm locally that scrollTranscriptItemToCenter computes the target as (segment position relative to container) - (container.clientHeight - segment.offsetHeight) / 2 and that tests assert this; include these files in the review evidence before closing the task."},{"id":"F2","severity":"SHOULD_FIX","title":"Coordinate basis must be the actual scroll container, not offsetParent","evidence":"The plan says positions are computed \"文字起こしコンテナ基準\"; if the helper uses element.offsetTop, that value is relative to the nearest positioned ancestor, which may not be scrollRef.current (e.g., if an intermediate wrapper or ScrollArea viewport is positioned), producing wrong scroll targets.","recommendation":"Prefer getBoundingClientRect-based math (segRect.top - containerRect.top + container.scrollTop) or verify offsetParent === container; also confirm scrollRef points at the element that actually has overflow scrolling, otherwise scrollTo is a no-op."},{"id":"F3","severity":"SHOULD_FIX","title":"Smooth-scroll behavior change and jsdom compatibility","evidence":"The removed code used scrollIntoView({ behavior: \"smooth\", block: \"center\" }); the diff does not show whether the helper passes behavior: \"smooth\" to scrollTo, and jsdom does not implement scrollTo with options (element.scrollTo may be undefined or throw in tests).","recommendation":"Preserve behavior: \"smooth\" (or record the UX change deliberately), and have the helper return/compute the target scrollTop as a pure function so tests can assert the number without relying on jsdom scroll APIs."},{"id":"F4","severity":"NOTE","title":"Test coverage: add bottom clamp and container-larger-than-content cases","evidence":"plan.md lists only \"中央寄せ座標と上端クランプ\" as test targets; segments near the end of the transcript and containers whose content fits without scrolling are common edge cases (browsers clamp scrollTop natively, but a pure helper returning the target value will not).","recommendation":"Add tests for max clamp (scrollHeight - clientHeight), zero/negative scroll range, and a segment taller than the container."},{"id":"F5","severity":"NOTE","title":"Auto-follow can still fight manual user scrolling within the pane","evidence":"The effect fires on every activePlaybackIndex change during playback and unconditionally recenters, so a user scrolling the transcript to read earlier content will be yanked back on the next segment change; this pre-exists the change and is arguably out of scope per plan.md.","recommendation":"No action required for this task; note it as residual behavior, or consider suppressing follow-up while the user is actively scrolling in a follow-up task."}],"local_verification":["Run the new unit test: e.g. `pnpm --filter dashboard test tests/test_transcript_scroll.test.ts` (or the project's vitest/jest equivalent)","Run dashboard typecheck, lint, and build as required by the verification contract","Manually verify in the browser: click a late transcript segment during playback and confirm the meeting-wide seek bar stays visible and window.scrollY is unchanged (compare before/after)","Inspect services/dashboard/src/lib/transcript-scroll.ts to confirm rect-based coordinates, smooth behavior, and top/bottom clamping","Confirm scrollRef.current is the element with overflow-y: auto/scroll (not an outer wrapper) via devtools"]}

fastapi>=0.128.8
uvicorn>=0.39.0
httpx>=0.28.1
h11>=0.16.0                       # CVE-2025-43859
idna>=3.15
pydantic>=2.4.0
python-dotenv>=1.2.1
# main.py passes FastApiMCP(headers=...), added in 0.4.0, and imports mcp.types.
# fastapi-mcp 0.4.x calls Server(name, description); mcp 2.0 dropped that
# signature, so an unbounded resolve breaks the image at import time.
fastapi-mcp>=0.4.0,<0.5
mcp>=1.12,<2

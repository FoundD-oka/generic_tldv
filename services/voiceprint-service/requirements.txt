# Torch CPU wheel strategy: pull torch/torchaudio from the official PyTorch
# CPU index instead of default PyPI. This avoids bundling ~2GB of CUDA
# runtime libs into an image that only ever runs ECAPA-TDNN inference on
# CPU. `--extra-index-url` (not `--index-url`) keeps PyPI as the primary
# index for every other package below.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchaudio>=2.2.0
speechbrain>=1.0.0

fastapi>=0.128.8
uvicorn[standard]>=0.39.0
h11>=0.16.0                       # CVE-2025-43859 (uvicorn transitive)
python-multipart>=0.0.20
numpy>=1.22.0,<2.0.0
soundfile>=0.12.0
